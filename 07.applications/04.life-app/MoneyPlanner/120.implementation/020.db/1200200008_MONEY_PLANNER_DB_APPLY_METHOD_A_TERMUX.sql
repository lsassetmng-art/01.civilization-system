-- ============================================================
-- MONEY PLANNER DB APPLY METHOD A TERMUX
-- 佐藤（DB担当）レビュー前提
-- 実行方式メモ
-- ============================================================

-- 実適用時は以下ルール
-- psql "$DATABASE_URL" <<'SQL'
--   [DDL/INDEX/VIEW]
-- SQL

-- このファイルは実行スクリプトではなく、Method A 運用固定メモ。
