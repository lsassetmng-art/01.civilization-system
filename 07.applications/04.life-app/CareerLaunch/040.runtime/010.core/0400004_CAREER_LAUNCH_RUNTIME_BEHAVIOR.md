# CareerLaunch RUNTIME BEHAVIOR
## Runtime rules
- モバイルは local draft save を即時実行し、同期は後続。
- sync_state を画面に表示する。
- conflict 時は silent overwrite 禁止。
- 面接当日モードはネット不安定でも直前要約とチェックリストを表示可能にする。
- ペルソナ / 背景設定はオフラインでも保持する。
