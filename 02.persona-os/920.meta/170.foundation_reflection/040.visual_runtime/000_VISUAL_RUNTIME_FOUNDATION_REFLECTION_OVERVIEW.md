# ============================================================
# VISUAL RUNTIME FOUNDATION REFLECTION OVERVIEW
# ============================================================

status: canonical-meta
system: persona-os
layer: meta
domain: foundation-reflection
subdomain: visual-runtime
owner: Boss
prepared_by: Zero

purpose:
Summarizes the PersonaOS-foundation-side reflection patch set
that applies PersonaOS normalized authority boundaries and
stage-separation rules to VisualRuntime as an internal execution layer.

position:
VisualRuntime is a PersonaOS foundation-side visual execution layer.
It is not an external authority peer and not an external OS-level target.

non_negotiable_reflection_rules:
- VisualRuntime must not become canonical visual truth authority
- VisualRuntime must not become identity/state/growth/snapshot authority
- render/session/cache/buffer/output data must remain non-authoritative
- render execution must remain distinct from canonical truth ownership
- visual runtime convenience must not bypass security/governance/canonical rules

patch_strategy:
- patch foundation-side design by reflection notes first
- absorb into PersonaOS foundation/visual/runtime-related canonical docs later
- explicitly separate canonical visual truth from execution artifacts
