# ============================================================
# STATIC ART ORIGIN COMMON COMPONENT HANDOFF
# ============================================================

status: canonical
owner: Boss
prepared_by: Zero

purpose:
  - StaticArtOS 起点の共通部品群を application common 側へ引き継ぐ
  - 今後はこのフォルダを共通部品正本として参照する

origin_side_role:
  - StaticArtOS 側 = 起点・候補・監査・反映メモ・後続監査

application_common_side_role:
  - application common 側 = 共通部品正本・公式選定・共有採用対象

handoff_judgment:
  - StaticArt由来の current wave 共通部品群は application common 側へ正本移管可能
  - later-wave 候補はまだ移管しない
  - same-family resolution は application common 側でも維持する

primary_reference_now:
  - 07.applications/00.application-common-components
