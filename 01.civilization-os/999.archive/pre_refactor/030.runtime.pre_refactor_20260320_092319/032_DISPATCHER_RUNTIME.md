# DISPATCHER RUNTIME

purpose:
Dispatch runtime work to the correct subsystem.

dispatch targets:
event processor
state engine
relation engine
metric engine
snapshot engine
scenario runner

rule:
dispatch must be deterministic and auditable
