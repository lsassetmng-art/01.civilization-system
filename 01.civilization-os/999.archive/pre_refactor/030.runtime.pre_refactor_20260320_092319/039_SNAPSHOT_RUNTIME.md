# SNAPSHOT RUNTIME

purpose:
Capture runtime snapshots.

snapshot triggers:
tick end
critical event
manual snapshot
branch checkpoint
terminal state

rule:
snapshots must be consistent and replayable
